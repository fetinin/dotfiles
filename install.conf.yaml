- clean: ["~"]

- link:
    ~/.profile: profile
    ~/.bash_profile: bash_profile
    # git
    ~/.gitconfig: gitconfig
    # ssh
    ~/.ssh/config:
      path: ssh/config
      create: true
    # shell
    ~/.zshrc: zshrc

- create:
    - ~/.antigen

- brew:
    - pipx
    - antigen
    - docker
    - lazydocker
    - speedtest-cli
    - k9s
    - golang
    - kubectl
    - minikube
    - bat
    - eza
    - fzf
    - htop
    - colima
    - d2
    - jq
    - jd

- tap:
    - homebrew/cask-fonts

- cask:
    - visual-studio-code
    - font-fira-code
